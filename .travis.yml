language: node_js
sudo: required
node_js:
- "10"
cache:
  yarn: true
  directories:
  - node_modules
  - "$HOME/google-cloud-sdk/"
before_install:
- openssl aes-256-cbc -K $encrypted_9dd4c22169b6_key -iv $encrypted_9dd4c22169b6_iv -in deployments.tar.enc -out deployments.tar -d
- tar xvf deployments.tar
- gcloud version || true
- if [ ! -d "$HOME/google-cloud-sdk/bin" ]; then rm -rf $HOME/google-cloud-sdk; export
  CLOUDSDK_CORE_DISABLE_PROMPTS=1; curl https://sdk.cloud.google.com | bash > /dev/null; fi
- source /home/travis/google-cloud-sdk/path.bash.inc
- gcloud components install kubectl || true
install:
- yarn install
- yarn lint
deploy:
- provider: script
  skip_cleanup: true
  script: bash ./deploy.sh production
  on:
    branch: master
- provider: script
  skip_cleanup: true
  script: bash ./deploy.sh staging
  on:
    branch: staging
